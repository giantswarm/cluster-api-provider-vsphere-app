name: cluster-api-provider-vsphere
namespace: giantswarm
serviceType: managed

project:
  branch: "[[ .Branch ]]"
  commit: "[[ .SHA ]]"

image:
  registry: quay.io
  name: giantswarm/cluster-api-vsphere-controller
  tag: v0.8.1

ports:
  healthz: 9440
  metrics: 8080
  webhook: 9443
